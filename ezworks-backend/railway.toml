[build]
builder = "NIXPACKS"

[deploy]
startCommand = "java -Dserver.port=${PORT} -jar target/ezworks-backend-0.1.0-SNAPSHOT.jar"
healthcheckPath = "/health"
healthcheckTimeout = 300
restartPolicyType = "ON_FAILURE"
