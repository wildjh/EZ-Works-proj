package com.ezworks.repository;

import com.ezworks.domain.user.AccionAdmin;
import org.springframework.data.jpa.repository.JpaRepository;

public interface AccionAdminRepository extends JpaRepository<AccionAdmin, Long> {
}
